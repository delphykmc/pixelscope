# PixelScope v0.1.0

Release date: 2026-08-26  
Source commit: `{{SOURCE_COMMIT}}`

## User-visible changes

- Windows desktop image inspection and comparison with synchronized 1/2/4/6-image views.
- Statistics, Histogram, Line Profile, Difference, Split Channels, Gray/Bayer/RAW inspection, and display-only digital gain workflows.
- Large selection paging, Pick/Keep curation, Sessions, Recent items, runtime settings, diagnostics, and bounded source/cache management.
- Remote IQA result browsing, submission/shared-storage integration, viewer-linked Scene inspection, historical-result workflow, and schema-v2 comparison semantics implemented on the repository side.
- Windows PyInstaller `onedir`, deterministic portable ZIP, and per-user Inno Setup installer distribution foundations.

## Packaging and validation

The release candidate is built on the authorized Windows development PC from the exact source commit above. Repository validation, canonical PyInstaller build/validation/smoke, portable ZIP build/smoke, production installer build, disposable-AppId installer install/run/uninstall smoke, and strict final bundle validation must all PASS before the candidate is staged.

The rendered candidate provenance records the actual Python/PyInstaller/Inno compiler identity and SHA-256 hashes for the staged production artifacts.

## Known deferred items

- P5-G real external GPU/SMB validation remains pending the authoritative external environment.
- P6 Identity & Access / corporate SSO integration remains externally gated and is not implemented by this release-candidate phase.
- Production signing/certificate policy and final SSO-aware clean-PC qualification remain P7-E work after P5-G/P6.
- Production GitHub Release upload is outside repository automation authority; an authorized user performs the approved manual publication step across the corporate security boundary.

## Publication note

This file is the durable release-note source. `scripts/build_release_candidate.py` replaces the single source-commit marker above with the exact candidate commit and writes a rendered `RELEASE_NOTES.md` beside the staged release artifacts and provenance. Publication remains an explicit authorized human action.
