name: User Guide validation

on:
  push:
    paths:
      - "docs/**"
      - "mkdocs.yml"
      - "requirements/docs.txt"
      - "scripts/check_docs.py"
      - "tests/unit/test_docs_contract.py"
      - ".github/pull_request_template.md"
      - ".github/workflows/user-guide.yml"
  pull_request:
    paths:
      - "docs/**"
      - "mkdocs.yml"
      - "requirements/docs.txt"
      - "scripts/check_docs.py"
      - "tests/unit/test_docs_contract.py"
      - ".github/pull_request_template.md"
      - ".github/workflows/user-guide.yml"

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
          cache: pip
          cache-dependency-path: requirements/docs.txt
      - name: Install documentation validation dependencies
        run: >-
          python -m pip install -r requirements/docs.txt
          pytest==7.4.4 ruff==0.1.15
      - name: Check installed documentation environment
        run: python -m pip check
      - name: Check documentation contract
        run: python scripts/check_docs.py
      - name: Run documentation contract test
        run: python -m pytest -q tests/unit/test_docs_contract.py
      - name: Check documentation checker style
        run: |
          python -m ruff check scripts/check_docs.py tests/unit/test_docs_contract.py
          python -m ruff format --check scripts/check_docs.py tests/unit/test_docs_contract.py
      - name: Check branch diff for whitespace errors
        run: |
          git fetch origin main:refs/remotes/origin/main
          git diff --check origin/main...HEAD
      - name: Build searchable offline-capable site
        run: python -m mkdocs build --strict
